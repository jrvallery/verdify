apiVersion: 1

providers:
  - name: Verdify Greenhouse
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
    folder: Greenhouse
    folderUID: greenhouse
