http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.verdify.ai`)"
      entrypoints:
        - websecure
      service: "api@internal"
      middlewares:
        - dashboard-auth
      tls: {}

  middlewares:
    dashboard-auth:
      basicAuth:
        users:
          - "admin:$apr1$iRO9pYm9$07od.XTFHf5lF14oS7pqP0"
