Agent Prompt — Verdify MVP “API Green” Milestone

Role: Senior agentic coder implementing Verdify’s MVP per API.md & DATABASE.md v2.0.
Goal: Implement backend API and DB so all contract tests pass; deliver logs, manifests, and test reports.
Scope anchors: The authoritative requirements are the two files supplied by the user (requirements/API.md, requirements/DATABASE.md) plus the template alignment text embedded within them.

Non‑negotiables:

All endpoints are prefixed /api/v1.

Auth: JWT for users; X‑Device‑Token for devices.

Metric units; UTC ISO‑8601 timestamps with Z.

Deterministic ETag on Config/Plan GET with If-None-Match → 304.

Standardized ErrorResponse shape and error codes.

DB: PostgreSQL + TimescaleDB; tables, constraints, and hypertables per spec.

Tests: pytest + FastAPI TestClient; Timescale test container in tests.

Housekeeping you must do throughout:

Maintain /docs/runbook/worklog.md (time-stamped bullets per task).

Maintain /docs/runbook/manifest.json (files created/modified per task).

Save test reports to /docs/test-reports/ (pytest-junit.xml, coverage).

Ensure every endpoint logs X-Request-Id; keep sample logs in /docs/log-samples/.

T00 — Repo Sanity & Scaffolding (Predecessor for all)

Implementation guidance

Ensure standard backend layout (create if missing):

backend/app/
  api/routes/          # routers grouped per API.md
  core/                # config, logging, security deps, db session, etag helpers
  models/              # SQLModel ORM classes
  schemas/             # Pydantic I/O schemas (if not reusing SQLModel)
  services/            # publish, planning bridge, telemetry ingest helpers
  db/                  # alembic, seeders, migrations helpers
  main.py              # FastAPI app factory
  __init__.py
backend/tests/


Add developer tooling (if absent): pytest, coverage, testcontainers[postgresql], httpx, ruff (optional), mypy (optional).

Confirm backend/pyproject.toml has deps listed in requirements/API.md and add test deps:

pytest, pytest-asyncio, testcontainers[postgres], schemathesis (optional), dredd (optional).

Add ENV & .env.example with DB URLs and JWT secrets (do not commit secrets).

Files to touch

backend/pyproject.toml

backend/app/main.py

backend/app/core/{config.py,logging.py,db.py,errors.py,security.py,etag.py}

backend/tests/conftest.py

docs/runbook/{worklog.md,manifest.json} (create)

Success/tests

pytest -q runs a trivial smoke test in backend/tests/test_smoke.py (create) to assert app starts and returns 200 on /api/v1/health.

Worklog + manifest initialized.

T01 — Requirements Reconciliation Snapshot

Implementation guidance

Create a short, committed spec‑diff to disambiguate minor drifts between sections in the requirements:

Controller label vs name: Use label (API.md authoritative). Expose fw_version, hw_version, last_seen.

Device token table naming: Use controller_token as in DATABASE.md §2.2 (hashed tokens with expiry & revocation).

ETag strength: use weak tag prefix W/"config:v{n}:{sha}" & W/"plan:v{n}:{sha}" per API.md examples.

Files to touch

docs/decisions/0001-requirements-reconciliation.md

Success/tests

Document checked in; referenced by future PRs. No runtime tests.

T02 — Database DDL & Alembic Migrations (Timescale + Core Tables)

Implementation guidance

Initialize Alembic (if not yet).

Encode exact DDL from requirements/DATABASE.md (first half; v2.0 authoritative) into migrations:

Extensions: timescaledb, pgcrypto.

Enums and meta tables (sensor_kind_meta, actuator_kind_meta) + seed data.

Core tables: app_user, greenhouse, zone, crop, zone_crop, zone_crop_observation, controller, controller_token, sensor, sensor_zone_map, actuator, fan_group, fan_group_member, controller_button, state machine tables, plan*, hypertables: sensor_reading, actuator_event, controller_status, input_event, config snapshots.

RLS helper functions: app.current_user_id(), app.current_controller_id() (stub policies added in T10).

idempotency_key, audit_log.

Create hypertables & policies in migration upgrade().

Add indexes and partial uniques per spec.

Files to touch

backend/app/db/alembic/versions/*_initial_schema.py

backend/app/core/db.py (engine & session)

backend/app/models/* (scaffold SQLModel classes aligned with DDL; fill gradually)

Success/tests

DB migration test: pytest backend/tests/test_db_migration.py spins PostgresContainer("timescale/timescaledb:latest-pg15") and runs alembic upgrade head without errors.

Verify existence of key tables and partial unique indexes (asserts via SELECT).

T03 — Core App Factory, Error Model & Logging

Implementation guidance

Implement main.py with app factory:

Mount routers under /api/v1.

Add middleware: request id injection, structured logging, gzip, CORS.

Implement global error handler returning ErrorResponse:

{ "error_code": "E409_CONFLICT", "message": "...", "details": {...} }


Map FastAPI HTTPException/validation to standardized codes (API.md).

Implement /api/v1/health route and /api/v1/meta/* for sensor/actuator kinds (read from meta tables).

Files to touch

backend/app/main.py

backend/app/core/errors.py

backend/app/api/routes/health.py

backend/app/api/routes/meta.py

Success/tests

Tests for:

GET /api/v1/health → 200 shape {status:'ok', time:'...'}

GET /api/v1/meta/sensor-kinds includes seeded kinds.

Malformed payload → standardized ErrorResponse with appropriate error_code.

T04 — Security: JWT & DeviceToken Dependencies

Implementation guidance

Implement JWT issue/verify using secret & exp.

Implement DeviceToken auth:

Header: X-Device-Token.

On auth, hash incoming token (sha256 base64), lookup active (revoked_at IS NULL, expires_at > now()).

On successful auth, set PostgreSQL session vars: SET LOCAL app.current_user_id = ... or app.current_controller_id = ....

Add dependency functions: get_current_user(), get_current_controller().

Files to touch

backend/app/core/security.py

backend/app/core/config.py (secrets, token expiry)

backend/app/api/deps.py

Success/tests

Unit tests verifying:

Valid JWT → user id available in handlers.

Valid device token → controller id available; invalid/expired → E401_UNAUTHORIZED.

T05 — Auth Router (/auth)

Implementation guidance

Implement per API.md:

GET /auth/csrf (issue CSRF token for browsers; store server-side or sign stateless token).

POST /auth/register (CSRF-protected in browser contexts), creates app_user and returns JWT.

POST /auth/login (CSRF-protected in browser contexts), validates and returns JWT.

POST /auth/revoke-token (user logout—mark JWT jti in denylist in-memory for tests or short TTL).

Use standardized errors (409 on duplicate email; 401 on bad creds).

Files to touch

backend/app/api/routes/auth.py

backend/app/schemas/auth.py

backend/tests/test_auth.py

Success/tests

Tests per API.md examples.

Email duplicate → E409_CONFLICT.

Invalid login → E401_UNAUTHORIZED.

T06 — Onboarding Flow (/hello, claim, token-exchange, rotate, delete)

Implementation guidance

Routes:

POST /hello (unauth): validate device_name regex; record “hello” ping (ephemeral table or reuse controller pre-claim row with provisional claim_code); return {status:'pending'|'claimed', ...}.

POST /controllers/claim (User JWT): binds device_name to greenhouse, creates controller row, returns controller_uuid and token-exchange URL. Enforce owner & unclaimed rules.

POST /controllers/{controller_uuid}/token-exchange (unauth, single-use): verify device_name + claim_code; issue device_token (store hash in controller_token with expiry) and return initial ETags.

POST /controllers/{controller_uuid}/rotate-token (User JWT): revoke old, issue new.

DELETE /controllers/{controller_uuid} (User JWT): revoke tokens; delete controller.

Persist last_seen on successful device calls later.

Files to touch

backend/app/api/routes/onboarding.py

backend/app/services/tokens.py

backend/tests/test_onboarding.py

Success/tests

Contract tests from API.md:

Claiming rules & errors (E404_NOT_FOUND, E409_CONFLICT).

Token exchange idempotency (201/200 with same ETags).

Rotate/delete behavior.

T07 — Controllers & Plan Access (Config/Plan GET with ETag)

Implementation guidance

Config GET

GET /controllers/{controller_uuid}/config (Device or User JWT).

Build materialized config snapshot (call service from T09/T11), store version & etag in config_snapshot.

Return ETag: W/"config:v{n}:{sha}". If If-None-Match matches → 304.

Plan current GET

GET /greenhouses/{greenhouse_id}/plan/current (Device or User JWT).

Load current plan slice for now(); return ETag similar to config (based on greenhouse.latest_plan_version).

Files to touch

backend/app/api/routes/controllers.py (config)

backend/app/api/routes/plans_readonly.py (current plan)

backend/app/services/etag.py (helper)

backend/tests/test_etag.py

Success/tests

GET with matching If-None-Match → 304.

GET without match → 200 + ETag present.

T08 — CRUD: Greenhouses/Zones/Crops/ZoneCrops/Observations (owner RLS behavior validated later)

Implementation guidance

Implement standard CRUD routes per API.md with validation:

/greenhouses CRUD (clamp baselines within guard rails in handler or model validator).

/zones CRUD (unique (greenhouse_id, zone_number)).

/crops, /zone-crops, /zone-crop-observations CRUD.

Enforce “one active planting per zone” at API and DB (partial unique already present).

Use pagination (page, page_size) consistently.

Files to touch

backend/app/api/routes/greenhouses.py

backend/app/api/routes/zones.py

backend/app/api/routes/crops.py

backend/app/api/routes/zone_crops.py

backend/app/schemas/{greenhouse.py,zone.py,crop.py,zone_crop.py}

backend/tests/test_crud_core.py

Success/tests

Creating duplicate zone_number in same GH → E409_CONFLICT.

Ending planting and starting new succeeds; concurrent active fails.

T09 — Sensors, Mappings, Actuators, Fan Groups, Buttons

Implementation guidance

/sensors CRUD with rules:

include_in_climate_loop == true only for kind ∈ {temperature, humidity} AND sensor’s controller_id equals greenhouse’s climate controller; otherwise E409_CONFLICT.

/sensor-zone-maps CRUD:

Enforce unique (zone_id, kind) (DB index), allow multi‑zone per sensor.

/actuators CRUD:

Unique (controller_id, relay_channel) where channel not null.

/fan-groups & /fan-groups/{id}/members CRUD:

Duplicate member → E409_CONFLICT.

/controller-buttons CRUD with stage range checks.

Files to touch

backend/app/api/routes/{sensors.py,sensor_zone_maps.py,actuators.py,fan_groups.py,controller_buttons.py}

backend/tests/test_devices_and_mappings.py

Success/tests

Contract tests per API.md “Validation Rules” & examples: loop inclusion guard, mapping singleton per zone/kind, channel uniqueness.

T10 — State Machine (49× cells + fallback) & Publish Validations

Implementation guidance

Implement routes:

/state-machine-rows CRUD; /state-machine-fallback/{greenhouse_id} PUT.

Implement publish validations:

Server function to verify coverage (fn_validate_state_machine call) on publish.

Keep MUST_ON/MUST_OFF lists and fan-group on_count per spec.

Files to touch

backend/app/api/routes/state_machine.py

backend/app/services/state_validation.py

backend/tests/test_state_machine.py

Success/tests

Incomplete grid or missing fallback → publish returns error E409_CONFLICT (or dry-run false rejects; dry-run true returns errors/warnings).

T11 — Config Publish & Diff

Implementation guidance

/greenhouses/{id}/config/publish (User JWT):

Build canonical config JSON from DB (deterministic sort of arrays), compute SHA256, persist config_snapshot row (version++), update greenhouse.latest_config_version.

On dry_run=true, do not persist; return published=false and validation errors/warnings only.

/greenhouses/{id}/config/diff?from=&to=:

Human-readable JSON diff (e.g., deepdiff or custom). Keep output stable and compact.

Files to touch

backend/app/api/routes/config.py

backend/app/services/config_builder.py

backend/app/services/json_diff.py

backend/tests/test_config_publish.py

Success/tests

Publish increments version, writes snapshot, returns stable etag.

Diff returns coherent changes for a known modification (e.g., add actuator).

T12 — Plans CRUD (Planner/Admin) + Version Tracking

Implementation guidance

/plans POST/GET/GET-by-id/DELETE:

Validate timestamps within effective window and uniqueness of setpoint timestamps.

Clamp to guard rails (either at API or via fn_clamp_setpoints_to_rails).

On create, set greenhouse.latest_plan_version.

(Planner JWT can be simulated by an admin flag on user for now.)

Files to touch

backend/app/api/routes/plans.py

backend/app/services/plan_validation.py

backend/tests/test_plans.py

Success/tests

Examples from API.md: overlapping setpoints → E422_UNPROCESSABLE; values outside rails → accepted with clamp + warnings.

T13 — Telemetry Ingest (+ optional Idempotency Header)

Implementation guidance

POST /telemetry/sensors (device token): validate ownership, kinds, metric units, and timestamps with Z. Insert into hypertable. If Idempotency-Key header present, dedupe entries and return cached response.

POST /telemetry/actuators: accept ON/OFF edges only; wrong case → E400_BAD_REQUEST.

POST /telemetry/status: accept frame with stages/averages; update controller last_seen.

POST /telemetry/inputs: record button events.

POST /telemetry/batch: accept mixed payloads.

Files to touch

backend/app/api/routes/telemetry.py

backend/app/services/telemetry_ingest.py

backend/tests/test_telemetry.py

Success/tests

Contract tests from API.md “Telemetry acceptance”.

Status frame missing required field → E400_BAD_REQUEST.

Sensor writes visible via simple SELECT in test.

T14 — Row‑Level Security (RLS) Policies

Implementation guidance

Add RLS policies after routes are green to simplify debugging. Policies must restrict reads/writes to:

User JWT: rows owned by app.current_user_id() through FK chains.

Device token: rows of the controller’s greenhouse via app.current_controller_id().

Ensure backend sets SET LOCAL app.current_user_id or app.current_controller_id per request (done in T04).

Provide a switch to disable RLS in tests that don’t need isolation, but enable for RLS tests.

Files to touch

New migration *_rls_policies.py

backend/app/core/db.py (session hook to set settings)

backend/tests/test_rls.py

Success/tests

Attempt to access another user’s greenhouse returns E403_FORBIDDEN.

Device cannot access resources outside its greenhouse.

T15 — Standardized Error Codes & Exception Mapping

Implementation guidance

Centralize raising of HTTPException with standardized codes from API.md (E400, E401, E403, E404, E409, E412, E422, E429, E500, E503).

Wrap DB constraint violations (unique/partial unique) into E409_CONFLICT with meaningful details.

Files to touch

backend/app/core/errors.py (DB error translation)

Apply usage across all routes.

Success/tests

Duplicated zone (gh, number) raises E409_CONFLICT with { field: 'zone_number' } style detail.

T16 — OpenAPI Hygiene & Client Generation Smoke

Implementation guidance

Ensure tags/routers named as in API.md (auth, onboarding, config, planning, telemetry, greenhouses, zones, sensors, actuators, crops).

Verify /api/v1/openapi.json compiles; add minimal examples.

(Optional) Generate TS/Python client locally to confirm types (no need to commit generated code).

Files to touch

backend/app/main.py (metadata)

backend/tests/test_openapi.py

Success/tests

GET /api/v1/openapi.json → 200; schema includes security schemes JWT and DeviceToken.

T17 — Contract Tests: End‑to‑End (from API.md §7)

Implementation guidance

Implement the listed contract tests directly:

Identity & format (device_name lowercase, timestamps Zulu).

Uniqueness & singleton (zone uniqueness; climate controller singleton).

State machine coverage on publish.

Sensor mapping duplicates.

ETag behavior 304s.

Telemetry acceptance (case sensitivity, required fields).

Plan invariants (overlaps warn or reject as specified).

Use TestClient and Timescale PostgresContainer. Provide fixtures for user, greenhouse, climate controller, sensors, actuators.

Files to touch

backend/tests/test_contract_*.py

Success/tests

All tests pass locally with pytest -q and produce a junitxml report in /docs/test-reports/pytest-junit.xml.

T18 — Audit Logging (Minimum Viable)

Implementation guidance

For mutations in configuration entities, write audit rows with actor (user or controller), before/after JSON, and X-Request-Id.

Provide a fast helper decorator to capture diffs.

Files to touch

backend/app/services/audit.py

Hook into create/update/delete endpoints.

backend/tests/test_audit.py (smoke)

Success/tests

Mutating a greenhouse writes an audit row with non‑null actor and request id.

T19 — Final Sweep, Artifacts & Review Trigger

Implementation guidance

Run full suite with coverage:

pytest -q --junitxml=docs/test-reports/pytest-junit.xml --maxfail=1
coverage run -m pytest && coverage html -d docs/test-reports/coverage


Export OpenAPI:

Save /docs/openapi/openapi.json snapshot.

Update worklog and manifest (list of files changed per task).

Collect sample logs (one publish, one telemetry ingest) in /docs/log-samples/.

Success/tests

All tests green. Artifacts present:

/docs/test-reports/pytest-junit.xml

/docs/test-reports/coverage/index.html

/docs/openapi/openapi.json

/docs/runbook/worklog.md

/docs/runbook/manifest.json

/docs/log-samples/*.log

Milestone outcome

Reaches “MVP API green”: All contract tests pass; endpoints implement required behaviors; artifacts & logs captured. This triggers a full code review of all touched modules and audit logs.

Cross‑Cutting Implementation Notes

ETag helper: Compute over canonical JSON (sorted keys, stable array ordering) and prefix W/"config:v{n}:{sha}". Same for plan.

DB constraint → API error: Wrap IntegrityError → map unique_violation & check_violation to E409_CONFLICT or E422_UNPROCESSABLE with clear details.

Session context: In per‑request dependency, set SET LOCAL app.current_user_id = '<uuid>' or app.current_controller_id = '<uuid>' before DB ops to satisfy RLS (T14).

Pagination: Uniform query params page (1‑based), page_size (default 50, max 200). Return X-Total-Count.

Idempotency: Accept optional Idempotency-Key on telemetry endpoints; write to idempotency_key with 24h TTL.

Suggested Command Cheatsheet
# Migrations
alembic revision --autogenerate -m "initial schema"
alembic upgrade head

# Test (full)
pytest -q

# With testcontainers/Timescale
pytest -q -k "not slow"  # fast subset
pytest -q backend/tests/test_contract_identity.py

# Coverage & reports
coverage run -m pytest && coverage html -d docs/test-reports/coverage

Deliverables Checklist (attach in PR)

 All tasks T00–T19 completed in order (note any deviations in worklog).

 pytest all green; reports saved.

 OpenAPI snapshot saved.

 Worklog + manifest + log samples committed.

 Short readme snippet in PR describing how to run dev server, migrations, and tests.

Start Now

Begin with T00 and progress in order. Update /docs/runbook/worklog.md and /docs/runbook/manifest.json as you complete each task. When you finish T19 with all tests green, post the artifacts and signal milestone complete so we can schedule the code review.